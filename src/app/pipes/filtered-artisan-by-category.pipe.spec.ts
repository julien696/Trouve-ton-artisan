import { FilteredArtisanByCategoryPipe } from './filtered-artisan-by-category.pipe';

describe('FilteredArtisanByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredArtisanByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilteredArtisanByTopPipe } from './filtered-artisan-by-top.pipe';

describe('FilteredArtisanByTopPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredArtisanByTopPipe();
    expect(pipe).toBeTruthy();
  });
});
